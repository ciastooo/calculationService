name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Ls test
      run: ls -a
    - name: Tests
      run: docker-compose -f docker-compose.test.yml up
